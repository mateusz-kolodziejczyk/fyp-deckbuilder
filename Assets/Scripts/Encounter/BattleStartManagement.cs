using UnityEngine;

namespace Encounter
{
    public class BattleStartManagement : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
